.DS_Store
__pycache__/
*.pyc
.venv/
site/
# large local artifacts — never commit (output/ is ~61G)
output/
skyweave-last-online-test-frames/
tmp/
# broken convenience symlinks
/skyweave
/skyweave2
# AI-generated session logs, findings, and progress/handoff notes — never
# commit. They churn every session and can carry rig facts/credentials.
# Governed deliverables (reports, phase briefs, runbook, contracts) do NOT
# match these suffixes and stay tracked.
*.log
*_LOG.md
*_FINDINGS.md
*_PROGRESS.md
*_HANDOFF.md
# kept explicit — a setup guide that does not match the suffixes above
v2/docs/REMOTE_ACCESS_SETUP.md
