Skyweave2

Rewrite workspace for the Skyweave core.

Use the original skyweave repository as the characterization source and compare
new implementations against its golden artifacts before replacing behavior.

Initial golden artifact:
../golden/peak_baselines.json

Initial generator:
../v1/scripts/generate_golden_peaks.py
